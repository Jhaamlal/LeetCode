// https://leetcode.com/problems/missing-number/
function missingNumber(nums: number[]): number {
  // We will put cyclic sort in practice there Babu
  const swap = (
    array: Array<number>,
    firstIndex: number,
    secondIndex: number
  ) => {}

  return 2
}
